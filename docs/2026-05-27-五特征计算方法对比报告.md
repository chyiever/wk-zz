# 五特征计算方法对比报告

**日期**：2026-05-27  
**涉及文件**：
- `notebooks/2026-05-19-realdata_feature_dataset_build-3.ipynb`（以下简称 build-3）
- `notebooks/2026-05-06-batch_feature_extract_12folders_gpu.ipynb`（以下简称 batch）
- `notebooks/2026-05-27-feature_comparison_build3_vs_batch.ipynb`（对比验证 notebook）
- `src/fea_cpt_gpu/features.py`

---

## 1. 背景

两个 notebook 均用于从 TDMS 原始数据文件中提取以下五个特征，用于后续的断丝识别任务：

| 特征名 | 频带 | 物理含义 |
|---|---|---|
| `b_1k_10k__SC_mean` | 1–10 kHz | 谱心均值 |
| `b_1k_10k__C_f` | 1–10 kHz | 脊线曲率 |
| `b_1k_100k__epsilon_2x` | 1–100 kHz | 二次谐波频率偏差 |
| `b_1k_100k__SC_res_mean` | 1–100 kHz | 残差谱心均值 |
| `b_1k_100k__I_burst` | 1–100 kHz | 小波包突发指数 |

**问题**：两个 notebook 在特征计算的具体实现上是否存在差异？结果是否一致？

---

## 2. 两个 notebook 的数据处理流程对比

### 2.1 数据流程（完全一致）

两个 notebook 均采用相同的处理流程：

```
原始 TDMS 文件
    ↓ 读取长通道信号
整条信号去均值 + (1k, 95k) Hz 4阶零相位 Butterworth 带通滤波
    ↓ 预处理一次，复用
滑窗切割（20 ms 窗口，50% 重叠）
    ↓ 每窗口独立计算
对每个窗口构建 FeatureRecord → build_context → 提取5个特征
    ↓
输出到 CSV
```

关键参数（两者完全一致）：

| 参数 | 值 |
|---|---|
| `WINDOW_DURATION_S` | 0.02 s（20 ms） |
| `WINDOW_OVERLAP` | 0.50（50%） |
| `PREPROC_BAND` | (1000.0, 95000.0) Hz |
| 预处理滤波阶数 | 4 阶 Butterworth |
| 频带定义 | `b_1k_10k`: (1k, 10k)，`b_1k_100k`: (1k, 100k) |

> **澄清**：两个 notebook 均**不是**对整段信号计算特征，而是对滑窗后的每个 20 ms 窗口分别计算特征。

### 2.2 `build_params_for_band` 实现（完全一致）

两个 notebook 中均定义了相同的 `build_params_for_band` 函数，用于为每个频带生成 `FeatureParams`：

```python
def build_params_for_band(band, sample_rate):
    low, high = band
    nyq = sample_rate / 2.0
    low, high = _safe_band(low, high, nyq)
    span = max(high - low, 10.0)

    ridge_main = _safe_band(low, low + 0.65 * span, nyq)
    ridge_h2   = _safe_band(max(low * 2.0, low + 0.20 * span),
                             min(high * 2.0, nyq * 0.995), nyq)

    return replace(DEFAULT_FEATURE_PARAMS,
                   highpass_hz=1_000.0,
                   main_band_hz=(low, high),
                   ridge_main_search_hz=ridge_main,
                   ridge_h2_search_hz=ridge_h2,
                   n_jobs=1)
```

以 500 kHz 采样率为例，各频带的参数值：

| 频带 | `main_band_hz` | `ridge_main_search_hz` | `ridge_h2_search_hz` |
|---|---|---|---|
| `b_1k_10k` | (1000, 10000) | (1000, 6850) | (2800, 20000) |
| `b_1k_100k` | (1000, 100000) | (1000, 65350) | (20800, 200000) |

---

## 3. 特征计算代码对比

### 方法 A：build-3 notebook 自定义函数

build-3 在 Helpers 单元格中内联定义了五个特征的专用计算函数，再在 `compute_5_features_for_window` 中调用：

```python
def compute_5_features_for_window(window_signal, sample_rate, params_map):
    rec = FeatureRecord(signal=window_signal, sample_rate=sample_rate, ...)

    ctx_1k10k  = build_context(rec, params_map['b_1k_10k'])
    ctx_1k100k = build_context(rec, params_map['b_1k_100k'])

    return {
        'b_1k_10k__SC_mean':      _spectral_centroid_mean(ctx_1k10k.stft_freqs,
                                                           ctx_1k10k.stft_power,
                                                           ctx_1k10k.params.eps),
        'b_1k_10k__C_f':          _c_f_from_context(ctx_1k10k),
        'b_1k_100k__epsilon_2x':  _epsilon_2x_from_context(ctx_1k100k),
        'b_1k_100k__SC_res_mean': _sc_res_mean_from_context(ctx_1k100k),
        'b_1k_100k__I_burst':     _i_burst_from_context(ctx_1k100k),
    }
```

五个自定义函数源码：

```python
# SC_mean：STFT 功率谱的逐帧谱心均值
def _spectral_centroid_mean(freqs, power, eps):
    if power.size == 0:
        return 0.0
    numerator   = np.sum(freqs[:, None] * power, axis=0)
    denominator = np.sum(power, axis=0) + eps
    sc = numerator / denominator
    return float(np.mean(sc)) if sc.size else 0.0

# C_f：脊线频率轨迹的归一化曲率均值
def _c_f_from_context(context):
    eps = context.params.eps
    if len(context.ridge_f1) <= 2:
        return 0.0
    curvature = np.gradient(
        np.gradient(context.ridge_f1, context.stft_times + eps),
        context.stft_times + eps,
    )
    return float(np.mean(np.abs(curvature) /
                         (np.mean(np.abs(context.ridge_f1)) + eps)))

# epsilon_2x：二次谐波脊线相对频率偏差中位数
def _epsilon_2x_from_context(context):
    eps    = context.params.eps
    active = context.ridge_f1 > 0.0
    if not np.any(active):
        return 0.0
    diff_h2 = np.abs(context.ridge_f2 - 2.0 * context.ridge_f1)
    return float(np.median(diff_h2[active] / (context.ridge_f1[active] + eps)))

# SC_res_mean：残差 STFT 功率谱的逐帧谱心均值
def _sc_res_mean_from_context(context):
    eps = context.params.eps
    return float(_spectral_centroid_mean(
        context.stft_freqs, context.residual_power, eps))

# I_burst：小波包节点能量分布的突发指数
def _i_burst_from_context(context):
    eps        = context.params.eps
    total_wp   = float(sum(context.wavelet_node_energies.values())) + eps
    sorted_nodes = sorted(context.wavelet_node_energies.items())
    wp_prob    = np.asarray([e / total_wp for _, e in sorted_nodes], dtype=float)
    return float(np.max(wp_prob) / (np.median(wp_prob) + eps))
```

### 方法 B：batch notebook 调用 `compute_all_features`

batch notebook 直接调用 `src/fea_cpt_gpu/features.py` 中的 `compute_all_features`，对每个窗口计算全量特征后按键名取值：

```python
ctx_1k10k  = build_context(rec, params_map['b_1k_10k'])
ctx_1k100k = build_context(rec, params_map['b_1k_100k'])

result_1k10k  = compute_all_features(ctx_1k10k)
result_1k100k = compute_all_features(ctx_1k100k)

out = {
    'b_1k_10k__SC_mean':      float(result_1k10k.features['SC_mean']),
    'b_1k_10k__C_f':          float(result_1k10k.features['C_f']),
    'b_1k_100k__epsilon_2x':  float(result_1k100k.features['epsilon_2x']),
    'b_1k_100k__SC_res_mean': float(result_1k100k.features['SC_res_mean']),
    'b_1k_100k__I_burst':     float(result_1k100k.features['I_burst']),
}
```

`compute_all_features` 中对应的计算代码（`features.py`）：

```python
# SC_mean（L142–145）
sc = _spectral_centroid(context.stft_freqs, context.stft_power, eps)
# def _spectral_centroid(freqs, power, eps):
#     return (freqs[:, None] * power).sum(0) / (power.sum(0) + eps)
features["SC_mean"] = float(np.mean(sc)) if sc.size else 0.0

# C_f（L192–200）
curvature = np.gradient(
    np.gradient(context.ridge_f1, context.stft_times + eps),
    context.stft_times + eps,
) if len(context.ridge_f1) > 2 else np.zeros_like(context.ridge_f1)
features["C_f"] = float(np.mean(np.abs(curvature) /
                                (np.mean(np.abs(context.ridge_f1)) + eps)))

# epsilon_2x（L188）
diff_h2 = np.abs(context.ridge_f2 - 2.0 * context.ridge_f1)
active  = context.ridge_f1 > 0.0
features["epsilon_2x"] = float(
    np.median(diff_h2[active] / (context.ridge_f1[active] + eps))
) if np.any(active) else 0.0

# SC_res_mean（L229–232）
sc_res = _spectral_centroid(context.stft_freqs, context.residual_power, eps)
features["SC_res_mean"] = float(np.mean(sc_res)) if sc_res.size else 0.0

# I_burst（L246–252）
total_wp     = float(sum(context.wavelet_node_energies.values())) + eps
sorted_nodes = sorted(context.wavelet_node_energies.items())
wp_prob      = np.asarray([e / total_wp for _, e in sorted_nodes], dtype=float)
features["I_burst"] = float(np.max(wp_prob) / (np.median(wp_prob) + eps))
```

---

## 4. 逐特征差异分析

| 特征 | 差异类型 | 说明 |
|---|---|---|
| `SC_mean` | 空数组保护 | 方法 A 在入口加了 `if power.size == 0: return 0.0`；方法 B 无此保护，但对正常数据无影响 |
| `C_f` | 边界条件写法 | 方法 A：`if len <= 2: return 0.0`（提前返回）；方法 B：`... if len > 2 else zeros`（三元表达式）。逻辑等价 |
| `epsilon_2x` | 完全一致 | — |
| `SC_res_mean` | 完全一致 | — |
| `I_burst` | 完全一致 | — |

**总结**：两种方法在数学公式层面完全等价，仅在空值/边界保护的写法上存在无实质影响的形式差异。

---

## 5. 数值验证结果

使用数据文件：`0002341-500K-20260324T201052.395.tdms`（采样率 500 kHz，时长 10 s）

滑窗参数：20 ms 窗口，50% 重叠，共 **999 个窗口**

| 特征名 | 最大绝对差 | 最大相对差 | 完全相同窗口数 | 是否完全一致 |
|---|---|---|---|---|
| `b_1k_10k__SC_mean` | 0.00e+00 | 0.00e+00 | 999 / 999 | **是** |
| `b_1k_10k__C_f` | 0.00e+00 | 0.00e+00 | 999 / 999 | **是** |
| `b_1k_100k__epsilon_2x` | 0.00e+00 | 0.00e+00 | 999 / 999 | **是** |
| `b_1k_100k__SC_res_mean` | 0.00e+00 | 0.00e+00 | 999 / 999 | **是** |
| `b_1k_100k__I_burst` | 0.00e+00 | 0.00e+00 | 999 / 999 | **是** |

---

## 7. 真实数据 vs 训练集+测试集 特征分布对比

> 本节对应 `notebooks/2026-05-27-feature_comparison_build3_vs_batch.ipynb` 第13节，
> 对比图保存在 `outputs/feature_distribution_comparison_A_vs_B.png`。

### 7.1 两种数据集对比

| 维度 | 真实数据（方法A） | 训练集+测试集（方法B） |
|---|---|---|
| **数据来源** | FIP 24下午工况连续采集的 TDMS 信号 | BK、F130 等系列工况的混合文件 |
| **提取方式** | 从实际生产过程中按滑窗提取 | 人工标注构建，按标签分组 |
| **样本量** | 719,550 条 | 10,586 条（噪声 5,293 + 断丝 5,293） |
| **标签** | 无标签区分 | label=0 噪声，label=1 断丝 |
| **特征计算** | build-3 自定义函数 | batch `compute_all_features` |
| **频带** | `b_1k_10k` 和 `b_1k_100k` | 相同 |

**关键区别**：两种数据集的特征**计算方法完全等价**（见第2–6节验证），但**数据源完全不同**——
真实数据来自连续生产过程，训练集+测试集来自人工标注的混合工况文件。

### 7.2 定量统计指标

每个特征打印均值、标准差、偏度、峰度、P25/P50/P75 分位数，以及三组 KS 检验。
以下为核心数据汇总：

#### SC_mean（谱心均值，1–10 kHz）

| 组别 | n | 均值 | 标准差 | 偏度 | 峰度 | P50 |
|---|---|---|---|---|---|---|
| 真实数据 | 719,550 | 2,641 | 716.6 | 0.62 | -0.12 | 2,537 |
| 训练集+测试集: 噪声 | 5,293 | 6,924 | 606.4 | 0.84 | 0.53 | 6,783 |
| 训练集+测试集: 断丝 | 5,293 | 3,772 | 404.4 | 0.35 | -0.65 | 3,738 |

- KS(真实 vs 噪声): 0.9998, p<0.001 → **分布完全不同**
- KS(真实 vs 断丝): 0.7297, p<0.001 → **分布差异极大**
- KS(噪声 vs 断丝): 1.0000, p<0.001 → **完全可分**

**分析**：真实数据的 SC_mean 远低于训练集的噪声和断丝两组，说明真实工况信号的频谱重心
整体偏低。训练集的噪声组 SC_mean 反而最高（6,924），断丝组居中（3,772），
这与直觉上"断丝信号频率更高"相反，可能与训练集样本的工况特殊性有关。

#### C_f（脊线曲率，1–10 kHz）

| 组别 | n | 均值 | 标准差 | P50 |
|---|---|---|---|---|
| 真实数据 | 719,550 | 5,670,112 | 2,154,944 | 5,566,158 |
| 训练集+测试集: 噪声 | 5,293 | 3,201,332 | 895,050 | 3,174,379 |
| 训练集+测试集: 断丝 | 5,293 | 8,958,231 | 1,240,766 | 8,892,276 |

- KS(真实 vs 噪声): 0.6337 → **显著不同**
- KS(真实 vs 断丝): 0.7026 → **显著不同**
- KS(噪声 vs 断丝): 0.9911 → **完全可分**

**分析**：真实数据的 C_f 均值介于噪声和断丝之间，更接近噪声组。
真实数据分布的覆盖范围比训练集更宽（标准差 215万 vs 89万/124万），
说明真实工况的脊线曲率变化更丰富。噪声与断丝两组 C_f 几乎无重叠（KS=0.99），
是区分能力最强的特征之一。

#### epsilon_2x（二次谐波偏差，1–100 kHz）

| 组别 | n | 均值 | 标准差 | 偏度 | P50 |
|---|---|---|---|---|---|
| 真实数据 | 719,550 | 7.64 | 3.78 | -0.48 | 9.00 |
| 训练集+测试集: 噪声 | 5,293 | 0.97 | 0.49 | -0.45 | 1.00 |
| 训练集+测试集: 断丝 | 5,293 | 5.34 | 2.03 | 0.69 | 5.33 |

- KS(真实 vs 噪声): 0.8909 → **完全不同**
- KS(真实 vs 断丝): 0.4299, p<0.001 → **有差异但部分重叠**
- KS(噪声 vs 断丝): 0.9979 → **几乎完全可分**

**分析**：这是三个特征中真实数据与断丝组最接近的一个（KS=0.43）。
真实数据均值 7.64 高于断丝组的 5.34，说明真实工况的二次谐波偏差
整体比训练集的断丝样本更大。噪声组的 epsilon_2x 极低（均值 0.97），
与真实数据和断丝组完全分离。

#### SC_res_mean（残差谱心均值，1–100 kHz）

| 组别 | n | 均值 | 标准差 | 偏度 | 峰度 | P50 |
|---|---|---|---|---|---|---|
| 真实数据 | 719,550 | 9,149 | 4,880 | 3.07 | 12.30 | 7,937 |
| 训练集+测试集: 噪声 | 5,293 | 13,802 | 2,611 | 0.09 | -0.28 | 13,876 |
| 训练集+测试集: 断丝 | 5,293 | 6,810 | 765 | 1.25 | 6.25 | 6,769 |

- KS(真实 vs 噪声): 0.6828 → **显著不同**
- KS(真实 vs 断丝): 0.4406, p<0.001 → **有差异但部分重叠**
- KS(噪声 vs 断丝): 0.9649 → **高度可分**

**分析**：真实数据的 SC_res_mean 偏度高达 3.07、峰度 12.30，
呈现强烈的右偏厚尾分布，说明真实工况中存在少量残差谱心极高的异常窗口。
训练集噪声组的 SC_res_mean 最高（13,802），接近正态分布（偏度 0.09），
断丝组最低（6,810）。

#### I_burst（小波包突发指数，1–100 kHz）

| 组别 | n | 均值 | 标准差 | 偏度 | 峰度 | P50 |
|---|---|---|---|---|---|---|
| 真实数据 | 719,550 | 2,541,955 | 10,800,107 | 12.78 | 276.59 | 186,350 |
| 训练集+测试集: 噪声 | 5,293 | 77,030 | 133,765 | 2.19 | 5.06 | 4,482 |
| 训练集+测试集: 断丝 | 5,293 | 3,677,517 | 2,540,617 | 1.35 | 2.52 | 3,097,913 |

- KS(真实 vs 噪声): 0.4992 → **显著不同**
- KS(真实 vs 断丝): 0.5666 → **显著不同**
- KS(噪声 vs 断丝): 0.9518 → **高度可分**

**分析**：I_burst 是所有特征中真实数据偏度（12.78）和峰度（276.59）最极端的，
呈现极度右偏的长尾分布——绝大多数窗口的 I_burst 值很低（P50=186,350），
但存在少量极端高值拖高了均值（2,541,955）。
训练集噪声组的 I_burst 最低（均值 77,030），断丝组最高（均值 3,677,517），
两者差异显著（KS=0.95）。真实数据的中位数（186,350）介于两者之间但更接近噪声组。

### 7.3 分布差异原因分析

两种数据集的特征分布存在显著差异，主要原因如下：

1. **数据源工况不同**
   - 真实数据来自 FIP 24下午连续生产过程，涵盖了设备运行中的各种工况变化
     （启动、稳定运行、载荷波动等），样本量极大（719,550条）。
     其中断丝信号仅有**几十条**，占比极小（<0.01%），其余几乎全部为噪声。
     因此**真实数据的整体分布本质上等同于噪声样本的分布**。
   - 训练集+测试集来自 BK、F130 等多系列工况的混合文件，经过人工筛选和标注，
     样本量较小（10,586条），且噪声和断丝样本数量严格相等。

2. **样本选择偏差**
   - 训练集的噪声和断丝样本是经过人工挑选的"典型"样本，代表了标注者认为
     最具代表性的工况片段。真实数据则包含了生产过程中所有的窗口，
     其中绝大部分是正常工况（噪声），与训练集中"被挑选出来的噪声"定义不同。
   - 训练集中噪声/断丝各 5,293 条，是人工平衡的；真实数据中噪声占 99.99% 以上，
     断丝仅几十条。

3. **TDMS 文件差异**
   - 真实数据与训练集+测试集来自不同的 TDMS 源文件，不同文件的采样率、
     传感器配置、设备状态等可能存在差异。
   - 例如真实数据中的采样率为 500 kHz 和 200 kHz（v3 和 0523 目录），
     而训练集+测试集的文件采样率可能不同。

4. **特征分布的覆盖范围**
   - 真实数据的标准差普遍更大（如 C_f 标准差 215万 vs 89万/124万），
     说明真实工况的特征值变化范围更广。
   - I_burst 和 SC_res_mean 的极端偏度和峰度表明真实数据中存在
     训练集中未覆盖的极端工况窗口。

5. **特征计算方法的等价性**
   - 需注意：两种数据集的**特征计算方法完全等价**（第2–6节已验证），
     分布差异不是由计算方法不同导致的，而是纯粹的数据源差异。

### 7.4 模型泛化能力验证结果与下一步建议

#### 模型泛化验证结果

已在真实数据上验证了训练集训练的模型的泛化能力，结果见
`2026-05-25-realdata_label_prediction_cross_condition.ipynb` 单元格 7。
**验证结论：模型泛化能力严重不足。**

- 真实数据中实际断丝信号仅有**几十例**。
- 模型在真实数据上预测出了**几十万个正样本**，假阳性率极高。
- 这表明训练集上学到的决策边界在真实数据上完全失效。

**根因分析**：结合 7.2 节的定量统计结果，模型泛化失败的根本原因在于
**训练集与真实数据的特征分布差异过大**：

1. 训练集的"噪声"与真实数据的"噪声"（即真实数据整体）特征分布完全不同——
   所有 5 个特征的 KS 检验均显著（p<0.001），KS 统计量在 0.50~1.00 之间。
2. 训练集的"断丝"与真实数据中那几十条真正的断丝是否匹配也存疑——
   训练集断丝的特征均值与真实数据整体分布差距极大。
3. 模型在训练集上学到的"断丝"模式，可能在真实数据的大多数噪声窗口上
   也表现为高概率，导致大量假阳性。

#### 下一步建议

1. **~~在真实数据上添加标签~~**：真实数据约 70 万条中仅几十条断丝，
   其余全部为噪声，**整体分布本质上等同于噪声分布**。
   当前已有足够信息将真实数据视为噪声参考分布，无需全量标注。
   后续可重点标注那几十条断丝信号，用于验证训练集断丝样本的代表性。

2. **训练集代表性评估**：利用真实数据作为"参考分布"，评估训练集的
   特征分布是否覆盖了真实工况的范围。可从以下角度：
   - 计算训练集样本在真实数据各特征分位数范围内的覆盖率
   - 使用 MMD（Maximum Mean Discrepancy）或 Wasserstein 距离
     量化分布差异
   - 分析训练集中缺失的真实数据特征区域（out-of-distribution 检测）
   - 特别关注训练集"噪声"是否覆盖了真实数据（本质为噪声）的特征范围

3. **数据增强与采样策略**：如果训练集未能覆盖真实数据的某些特征区域，
   考虑：
   - 从真实数据中补充标注样本（特别是处于训练集分布边界附近的样本）
   - 使用数据增强技术扩展训练集的分布范围
   - 对训练集中过采样的区域进行降采样
   - 考虑使用真实数据中大量可用的噪声样本来扩充训练集的噪声类别

4. **模型泛化能力提升**：当前模型在真实数据上产生数十万假阳性，
   需要从根本上解决分布不匹配问题：
   - 考虑使用**域自适应**（Domain Adaptation）技术，将模型从训练集域
     迁移到真实数据域
   - 使用**对抗训练**或**分布对齐**方法缩小训练集与真实数据的分布差距
   - 在训练集中加入更多样化的噪声样本，使模型学习到更鲁棒的决策边界
   - 考虑使用**异常检测**而非分类方法：将问题重新定义为"是否偏离正常噪声分布"

5. **特征选择优化**：基于分布对比结果，考虑哪些特征在真实数据和
   训练集上的分布最接近，哪些差异最大，优先选择分布一致性好的特征
   用于最终的断丝识别模型。初步观察：
   - `epsilon_2x` 和 `SC_res_mean` 在真实数据与断丝组之间 KS 较低（0.43~0.44），
     分布重叠相对较多，可能在跨域场景下更稳定
   - `C_f` 和 `I_burst` 在噪声与断丝之间区分力强（KS>0.95），
     但在真实数据上的分布与训练集差异也较大，需要谨慎使用

---

## 8. 结论

1. **两个 notebook 的数据处理流程完全相同**：均对整条信号预处理一次，再对 20 ms / 50% 重叠的滑窗逐窗口提取特征，不存在"整段信号计算特征"与"滑窗计算特征"的差异。

2. **五个特征的计算公式完全等价**：build-3 的内联自定义函数是对 `compute_all_features` 中对应逻辑的提取复刻，两者数学上无差异。

3. **数值验证结果为零差异**：在 999 个窗口上，所有五个特征的绝对差均为 0，结果完全一致，符合预期。

4. **两个 notebook 的主要区别不在特征计算逻辑，而在工程结构上**：
   - build-3 只计算 5 个目标特征（轻量），并行粒度为窗口级，直接支持 TDMS 输入
   - batch 调用 `compute_all_features` 计算全量特征集，适合全特征探索场景

5. **真实数据与训练集+测试集的特征分布差异显著**：
   - 特征计算方法等价的前提下，分布差异源于数据源、样本选择偏差和工况覆盖范围的不同。
   - 真实数据的标准差普遍更大，I_burst 和 SC_res_mean 呈现极端偏态分布。
   - 训练集的噪声与断丝两组在所有特征上高度可分（KS > 0.95），但真实数据的分布
     往往跨越两者的范围，说明真实工况更加复杂多样。
   - 详见第 7 节的定量分析和下一步建议。
