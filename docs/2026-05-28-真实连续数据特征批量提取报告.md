# 2026-05-28 真实连续数据全量特征批量提取报告

## 1. 任务概述

### 1.1 目标

对连续真实数据（npz/tdms 文件）进行**滑窗全量特征提取**，支持：
- 批量处理多个文件夹中的数据文件
- 兼容 200kHz 和 500kHz 两种采样率（低采样率自动升采样到统一目标）
- 每个窗口计算所有频带的全量特征（约 80 特征/频带），而非选定子集
- 最大化 CPU/GPU 利用率，减少重复运算

### 1.2 与已有方案的对比

| 方案 | 输入类型 | 特征范围 | 输出粒度 | 适用场景 |
|------|----------|----------|----------|----------|
| batch_feature_extract_12folders_gpu | 离散 npz 样本 | 全量 (~564列) | 每样本一行 | 训练集/测试集批量提取 |
| realdata_feature_dataset_build-3 | 连续信号 | 5个选定特征 | 每窗口一行 | 连续数据快速特征筛选 |
| **本方案** | 连续信号 | **全量** | 每窗口一行 | 连续数据全量特征分析 |

## 2. 系统设计

### 2.1 架构概览

```
notebook (配置与编排)
    │
    ├── RAW_DATA_ROOTS: 数据路径列表
    ├── BANDS: 频带配置
    ├── WINDOW_DURATION_S / OVERLAP: 滑窗参数
    └── TARGET_SAMPLE_RATE: 统一采样率
    │
    ▼
sliding_window.py (核心模块)
    │
    ├── discover_source_files()      # 多路径文件发现
    ├── load_source_file()           # npz/tdms 加载
    ├── upsample_to_target()         # 升采样 (200k→500k)
    ├── butter_filter()              # 预处理滤波 (复用 fea_cpt_gpu)
    ├── list_window_ranges()         # 滑窗范围计算
    ├── build_params_for_band()      # 频带参数构建
    ├── compute_all_features_for_window()  # 全量特征计算
    └── build_sliding_window_dataset()     # 批量编排 + 分块输出
    │
    ▼
fea_cpt_gpu 底层模块 (复用，不修改)
    ├── base.py:        FeatureRecord, FeatureParams
    ├── signal_ops.py:  build_context, butter_filter
    ├── features.py:    compute_all_features (~80特征)
    └── gpu_backend.py: GPU SVD 加速 (Q_MP 特征)
```

### 2.2 数据流

```
源文件 (npz/tdms)
    │
    ▼  load_source_file()
原始信号 + 原始采样率
    │
    ▼  upsample_to_target()
    │   200kHz → 500kHz (resample_poly, up=5, down=2)
    │   500kHz → 500kHz (直通)
统一采样率信号
    │
    ▼  butter_filter() [整条信号一次]
    │   去均值 + 带通滤波 (PREPROC_BAND)
预处理信号
    │
    ▼  list_window_ranges()
    │   窗口时长 × 重叠率 → 窗口列表
    │   例: 20ms @ 500kHz = 10000样本/窗口, 步长5000 (50%重叠)
窗口切片
    │
    ▼  ThreadPoolExecutor (N workers) 并行处理
    │   每窗口: build_context × N_bands + compute_all_features × N_bands
特征字典 {band__feature: value}
    │
    ▼  组装 DataFrame + 元数据
分块 CSV 输出 (每 N 个文件一个 CSV)
```

### 2.3 文件组织

| 文件 | 用途 |
|------|------|
| `src/fea_cpt_gpu/sliding_window.py` | 滑窗核心模块（新建） |
| `notebooks/2026-05-28-realdata_continuous_feature_batch_extract.ipynb` | 交互式笔记本（新建） |
| `tools/build_sliding_window_notebook.py` | Notebook 生成脚本（新建） |
| `outputs/realdata_continuous_features_20260528/` | 输出目录 |

**设计原则：不修改 `src/fea_cpt_gpu/` 下任何已有源码文件**，仅新建 `sliding_window.py` 模块，通过 import 复用现有代码。

## 3. 关键设计决策

### 3.1 升采样策略

- **方法**：`scipy.signal.resample_poly`（多相滤波器组）
- **优势**：比 FFT-based `scipy.signal.resample` 更高效，数值稳定性好，适合长信号
- **比率计算**：通过最大公约数自动计算整数比
  - 200kHz → 500kHz: `up=5, down=2`（2.5倍）
  - 其他比率自动适配
- **采样率不变时**：直接返回原始信号，零开销

### 3.2 全量特征 vs 选定特征

build-3 仅计算 5 个选定特征（SC_mean, C_f, epsilon_2x, SC_res_mean, I_burst），本方案对每个频带调用 `compute_all_features()`，计算全部约 80 个特征：

- 包络特征：r_p, C_E, A_env, S_env, Sk_env, R_td, R_fb, C_bulge, N_bulge, epsilon_env, eta_bw
- 频谱特征：SC_mean, k_sc, R_hl_mean, k_hl, beta_H, H_tf, SF, H_alpha
- 脊线特征：rho_r, G_gap, R2_ridge, S_arch, H2_ratio, R_2_1, H_stack, R_h, epsilon_2x, C_f 等
- 残差特征：E_harm, E_res, rho_res, R_high_res, epsilon_rec, N_abn, F_peak, K_loc, SK_max 等
- 小波特征：R_wp_* (16节点), H_wp, I_burst, D_WPT
- 阻尼原子特征：C_damp, alpha_hat, Q_MP, Delta_J, eta_dict

6 个频带 × 80 特征 = 约 480 个特征列 + 约 20 个元数据列。

### 3.3 并行策略

- **窗级并行**：`ThreadPoolExecutor` 处理窗口，workers 数可配置
- **批量处理**：每 256 个窗口为一批，减少线程创建开销
- **预处理一次**：整条信号的去均值 + 带通滤波仅执行一次，不随窗口重复
- **参数缓存**：`build_params_for_band()` 按 `(采样率, 频带)` 缓存 FeatureParams

### 3.4 GPU 利用现状

GPU 仅用于 `Q_MP` 特征中的 SVD 计算（`torch.linalg.svd` on CUDA）。其余约 79 个特征均为 CPU 计算（NumPy/SciPy）。这是由 `fea_cpt_gpu` 底层架构决定的，本方案不做修改。

## 4. 性能分析

### 4.1 单窗口计算复杂度

每个窗口需要：
1. N_bands 次 `build_context()` 调用
2. 每次 `build_context()` 包含：带通滤波 × 3 + Hilbert 包络 + STFT × 2 + 脊线提取 (DP) + 谐波掩码 + ISTFT + 小波包分解
3. N_bands 次 `compute_all_features()` 调用（纯公式计算，耗时可忽略）

**主要瓶颈**：`build_context()` 中的 STFT 和脊线提取。每增加一个频带，计算量近似线性增长。

### 4.2 内存占用估算

- 500kHz 采样率，20ms 窗口 = 10,000 样本/窗口
- 单个 FeatureContext 峰值内存约 5-10MB（含 STFT 矩阵、小波系数等）
- N_bands = 6 时，单窗口峰值约 30-60MB
- 8 workers 并发：约 240-480MB，在合理范围内

### 4.3 升采样开销

200kHz → 500kHz 升采样 2.5 倍后：
- 数据量增加 2.5 倍
- 窗口样本数从 4,000 增加到 10,000
- 窗口数量也增加 2.5 倍（因为信号更长了）
- 总体计算量约增加 2.5-3 倍（升采样本身很快，瓶颈在后续特征计算）

### 4.4 断点续跑机制

- 使用 `processed_source_files.txt` 记录已处理文件的绝对路径
- 每次运行自动加载记录，跳过已处理文件
- 支持中断后继续，不重复计算

## 5. 输出规范

### 5.1 特征 CSV 列结构

| 列名 | 类型 | 说明 |
|------|------|------|
| `source_file_name` | str | 源文件名 |
| `source_file_path` | str | 源文件绝对路径 |
| `source_format` | str | npz 或 tdms |
| `source_group_name` | str | TDMS 组名 |
| `source_channel_name` | str | TDMS 通道名 |
| `source_detail` | str | TDMS 详细信息 |
| `window_id` | int | 窗口序号（文件内） |
| `window_start_index` | int | 窗口起始样本索引 |
| `window_end_index` | int | 窗口结束样本索引 |
| `window_length_samples` | int | 窗口长度（样本数） |
| `window_step_samples` | int | 步长（样本数） |
| `window_duration_s` | float | 窗口时长（秒） |
| `window_start_offset_s` | float | 窗口起始偏移（秒） |
| `window_start_datetime` | str | 窗口起始时间戳 |
| `sample_rate_hz` | float | **升采样后**的采样率 |
| `original_sample_rate_hz` | float | **原始**采样率 |
| `source_n_samples` | int | 升采样后总样本数 |
| `source_duration_s` | float | 升采样后总时长 |
| `starttime_raw` | str | 原始 starttime |
| `arrival_time_raw` | str | 原始到达时间 |
| `sample_type` | str | 样本类型 |
| `{band}__{feature}` | float | 各频带特征值 |

### 5.2 日志 CSV 结构

与特征 CSV 相同的元数据列 + `missing_selected_features` 列（记录计算异常信息）。

### 5.3 分块输出

- 每 `NPZ_PER_CSV`（默认 100）个文件输出一个 CSV
- 文件名格式：`features_part_0001.csv`, `features_part_0002.csv`, ...
- 第一个 chunk 写入表头，后续 chunk 追加数据（无表头）

## 6. 验证计划

### 6.1 模块导入验证

```python
from fea_cpt_gpu.sliding_window import (
    SlidingWindowConfig,
    build_sliding_window_dataset,
    discover_source_files,
    list_window_ranges,
    upsample_to_target,
    process_source_file,
    load_source_file,
)
```

### 6.2 升采样正确性

- 200kHz 信号升采样到 500kHz，长度比 = 2.5
- 500kHz 信号保持不变
- 频谱内容保持（无混叠）

### 6.3 单窗口全量特征数量

- N_bands 个频带 × ~80 特征/频带 = 总特征列数
- 特征命名格式：`{band_name}__{feature_name}`

### 6.4 跨采样率可比性

- 相同物理信号，200kHz 升采样后与原始 500kHz 的特征分布应高度一致

## 7. 已知限制与后续优化方向

### 7.1 跨频带中间结果未共享

每个频带独立调用 `build_context()`，STFT 会重复计算。例如 b_1k_100k 和 b_1k_10k 的 STFT 频带不同但底层信号相同，可以考虑缓存共享。

### 7.2 GPU 利用率偏低

仅 Q_MP 特征使用 GPU (SVD)，其余 79 个特征为 CPU 计算。如需更高 GPU 利用率，需要将 STFT、脊线提取等迁移到 GPU。

### 7.3 潜在优化方向

1. **STFT 缓存**：对同一窗口信号，计算一次宽频带 STFT，各频带从中提取子带
2. **文件级并行**：多文件夹可使用 ProcessPoolExecutor 并行处理
3. **增量窗口**：对新到达的数据仅处理新增窗口，不重算已有窗口
4. **内存优化**：超长信号可分段预处理，避免一次性加载全部数据

## 8. 结论

本方案在**不修改已有 `fea_cpt_gpu` 源码**的前提下，通过新建 `sliding_window.py` 模块实现了连续真实数据的滑窗全量特征批量提取。方案支持 200kHz/500kHz 统一采样率处理、窗级并行计算、断点续跑和分块输出，满足大批量连续数据分析需求。
