1.	时域特征
手动标记每个断丝信号的初至时间，当断丝信号的能量衰减到最大能量的1/e时认为断丝信号结束（滑窗长0.01ms，步长0.01ms）。由此计算断丝信号的时间宽度。
计算所有断丝样本的信号时间宽度，分析其统计学规律（均值，均方差，绘制柱状图观察分布规律）。
不同频带滤波时，断丝信号的峰峰值（均值，均方差）、最前面0.01s窗口的信号能量（均值，均方差）
2.	频域：
计算每次断丝信号的PSD，之后计算概率PSD，横轴代表频率，纵轴代表PSD幅值，使用颜色代表概率。在概率密度图中，绘制平均的PSD曲线（黑线），绘制静水噪声曲线（蓝线）。通过概率分布，观察多次断丝信号的PSD曲线形状是否一致。
3.	能量
计算每个断丝信号PSD在频率轴上的积分曲线，横轴频率，纵轴代表低于该频率的信号能量占总信号能量的占比。画出来的曲线应该是一种缓慢上升的曲线。
计算PSD积分曲线的概率分布，绘图表示。
4.	信噪比
计算静水噪声、0.52m/s、0.65m/s三种工况下断丝信号在不同频带上的信噪比。绘制柱状图，横轴代表不同的频带（100~1000Hz、1kHz~10kHz、10kHz~20kHz、20kHz~30kHz、30kHz~40kHz、40kHz~50kHz、50kHz~100kHz等），纵轴代表信噪比。
计算静水噪声、0.52m/s、0.65m/s三种工况下断丝信号在不同频点上的信噪比。绘制曲线图，横轴代表频率，纵轴代表每个频点的信噪比，三条曲线代表了三种工况。

图 4.2.8
1.	信噪比
计算静水噪声、0.52m/s、0.65m/s三种工况下断丝信号在不同频带上的信噪比。绘制柱状图，横轴代表不同的频带（100~1000Hz、1kHz~10kHz、10kHz~20kHz、20kHz~30kHz、30kHz~40kHz、40kHz~50kHz、50kHz~100kHz等），纵轴代表信噪比。
计算静水噪声、0.52m/s、0.65m/s三种工况下断丝信号在不同频点上的信噪比。绘制曲线图，横轴代表频率，纵轴代表每个频点的信噪比，三条曲线代表了三种工况。

1.1.1、0.52 m/s和0.65 m/s流速下的环境噪声
使用概率密度PSD的形式评估环境噪声PSD水平。读取一天中的多个文件，进行运算，汇总到PPSD一张图上，以确保结果具有代表性。





为了能够对md文档中介绍的几种特征进行分类，现在需要编写信号特征计算函数（覆盖md文档中提到的所有特征），首先提取所有样本的特征，之后进行特征重要性排序、特征降维，得到重要性较高的一些特征列表；之后按照上述特征列表，批量计算所有样本的特征向量，并存储；最后使用这些特征训练SVM、GMM等模型，并比较不同特征数量下模型的分类效果、不同模型的分类效果。细节如下：
（1）哪些特征：根据md文章中介绍的所有特征。按照模块化开发的原则，每个特征单独编写一个或多个函数，可以根据有无重复计算对特征进行分类，若有些特征中有重复的计算，可以将重复的部分提炼出来，减少运算量。确保开发的特征计算函数的通用性，可以用于其他信号特征提取任务。
（2）怎么计算：计算时可采用并行算法，提高计算速度，计算后注意保存所有的特征结果（注意明确样本id（即样本名）、样本类型编码、与特征向量的对应关系）。
（3）采用信号特征工程领域常用的2~3中方法，对特征进行排序，每种排序结果绘图显示并保存（前20种）。所有排序结果汇总到excel中。给每种排序方式一定的权重（例如三种方法，权重为0.3：0.3：0.4），得到平均的排序结果（存到单独一个excel文件）。
（4）构建样本集：
训练集与验证集：共xxx条，比例是7：3。
测试集：共xxx条。确保与训练集与验证集没有重复的样本。
（5）构造SVM、GMM、决策树、CNN等5~7种多分类模型，给出每种需要调整的参数范围（每种模型单独整理到一个文件/notebook表格，便于统一修改），使用上述特征训练模型（注意为每次训练试验进行技术存档，包括完整的参数设置、相关源码、训练过程的日志、训练结果测试结果的图表等）。
（6）每种模型挑选出一组最佳的结果，对比不同模型最佳结果差异（图、表）。
开发要求：
（a）(1)~(3)专门新建一个文件夹fea_cpt，存储相关的py源程序；另外新建一个notebook文件用于调用py源程序进行特征计算、绘图，notebook文件中应具有足够多可以调整计算的特征类型、信号在特征计算前参与的预处理参数、各个环节结果保存的控制（路径、文件名等），各个绘图的原始代码（便于手动调整）、打印关键的结果（例如前20个重要的特征、计算特征时的进度条）
（b）（4）专门新建一个文件夹sample_cte,存储相关的py源程序；另外新建一个notebook文件用于调用py源程序进行样本集划分。
（c）(5)~(6)专门新建一个文件夹model_test,存储相关的py源程序；另外新建2个notebook文件,一个用于调用py源程序训练模型，存储结果，另一个用于绘图查看每个模型的训练结果，对比不同模型的结果。根目录下建立专门的文件夹model存储每次的模型，建立test_results文件夹，存储每种测试集下每个模型的测试结果（暂时只有一种测试集）。
（d）开发过程注意编写文件级、函数级、语句级日志，注意模块化开发，注意程序的可扩展性、简洁性和可维护性。



1. 将24个断丝样本分为两组：第一组16个（8个切割断丝+8个腐蚀断丝），记作BK14_tr，作为训练集的原始断丝样本；第二组8个（4
  个切割断丝+4个腐蚀断丝），记作BK14_te，作为测试集的原始断丝样本。
  2. 将F130数据集,按照训练集：测试集=7：3的比例，划分为两组，分别记为F130_tr、F130_te。
  3. 构建平坦流噪声环境下的断丝样本集：将F130_tr中的样本与BK14_tr中的样本随机组合、相加，得到平稳环境下的断丝样本集训练
  集，记作BK_f130_tr；将F130_te中的样本与BK14_te中的样本随机组合、相加，得到平稳环境下的断丝样本集测试集，记作
  BK_f130_te。BK_f130_tr与F130_tr数据集的样本数量相同，BK_f130_te与F130_te数据集的样本数量相同.
  4. 3中所述“随机组合、相加”的具体步骤：构建时，随机抽取signal1为F130_tr中的一信号(时长0.02s，采样率200kHz或500kHz)，随
  机抽取signal2为BK14_tr中的一信号(时长0.06s，采样率200kHz或500kHz)，首先统一采样率至200kHz，之后从signal2中截取0.02s的
  时窗得到signal3，此时signal1和signal3应该采样率和采样点数目一致，计算signal4 = w1×signal1+w2×signal3，w1和w2为0.5~1.5
  之间的随机数（小数点后保留2位），两者不一定相等，signal4即输出的BK_f130_tr数据集中的新样本。从signal2中截取0.02s长的
  时窗得到signal3的步骤如下：从signal2数据文件的头文件中获取初至时间，将初至时间加上t秒后的时间作为0.02s窗口的起始时
  间，t为[-0.005, 0.005]s之间的随机数，且t是0.0001的整数倍。将signal4存储到本地BK_f130_tr文件夹中，格式为npz，文件格式
  与signal2等保持一致，在头文件中添加组合所用的数据来源、w1、w2、t等组合参数;signal4的文件名在signal2文件名基础上修改，
  格式例如：signal2文件名-w1-w2-t-随机一位小写字母.npz，注意修改文件名中的采样率。生成该样本后，编写数据集构建日志：
  dataset_bulid_log.csv,记录新的样本所属的数据集名称、组合使用的两个原始数据文件名、新的样本文件名、w1、w2、t等组合参
  数、采样率、时长等。
  5. 类似2、3、4的方式，构建F130A_tr、F130A_te、BK14_f130a_tr、BK14_f130a_te、F130C_tr、F130C_te、BK14_f130c_tr、
  BK14_f130c_te数据集。
  6. 新建dataset_build_260505文件夹，为每个数据集新建1子文件夹，文件夹名即样本集名。
  7. 注意编写dataset_bulid_log.csv和dataset_bulid.md文档，md文档中详细记录构建过程。
  8. 在src中新建dataset_build子文件夹，编写相应的程序（py），在notebooks中新建dataset_build.ipynb文件，调用相关函数，实
  现数据集构建，相应的数据集名称、路径输入、训练集测试集比例、每个数据集数量统计打印、进度条、等等，都需要在ipynb中控制
  或显示。
  9. 模块化开发、编写各级注释、注意使用UTF-8，中文字符不要乱码。

  请先不要直接进行开发，先分析上述我的需求和方案是否合理可行？








现在已经提取出来了每个数据集的特征，下一步应该做什么？我最终的目的是实现跨工况的断丝  信号识别。
下一步不是马上上复杂模型，而是先把“跨工况”评估协议定死，再做基线与泛化优化。
  建议按这个顺序做：
  1. 定义跨工况评估协议（最关键）
     用 Leave-One-Condition-Out：每次留一个工况做测试，其余工况训练。
     例如：训练 F130+F130A，测试 F130C、F130、F130A；训练 F130，测试  F130C、F130、F130A；训练  F130+F130A+F130C，测试  F130C、F130、F130A，等等，累计6+1组。
     判断是否真的“跨工况”。
  2. 先做稳健基线模型
     先跑 Logistic / SVM(RBF) / RandomForest / XGBoost 四类，统一预处理：
     训练集拟合标准化、缺失值处理、类别权重。
     指标至少看：F1、Recall(断丝)、AUROC、FPR@TPR.
  3. 做特征稳定性筛选（跨工况导向）
     不只看重要性，还看“在不同工况下分布是否稳定”。
     去掉工况敏感但分类贡献小的特征，保留跨域稳定特征子集。
  4. 做域泛化/域对齐（第二阶段）
     在基线后加：CORAL 或特征白化对齐，再比较跨工况提升。
     如果提升明显，再考虑更复杂的方法（DANN/深度模型）。
  5. 阈值与部署策略
     最终按业务目标定阈值（比如优先高召回断丝），
     输出：模型文件 + 标准化参数 + 特征列表 + 固定阈值。

  ———
  实验、指标汇总表、混淆矩阵和最优特征子集导出）。
请给出1个特征筛选与信号分类方案，编写md文档，格式、结构参照2026-5-5-可执行的数据集构建设计说明.md。